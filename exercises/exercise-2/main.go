package main

import "fmt"

var x int
var y string
var z bool

func main() {
	//  USE var to DECLARE three variables. The variables should have package level scope.
	// Do not assign VALUES to the variables. Use the following IDENTIFIERS for the VARIABLES
	// and make sure the variables are of the following TYPE(meaning they can store VALUES of that TYPE)
	// a. identifier "x" type int
	// b. identifier "y" type string
	// c. identifier "z" type bool
	// in func main print out the values for each identifier

	fmt.Println(x)
	fmt.Println(y)
	fmt.Println(z)
}
